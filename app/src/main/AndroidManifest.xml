<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.timememo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Users.AccountManagerActivity"></activity>
        <activity android:name=".LockPhone.LockTimeActivity" />
        <activity android:name=".HistoryLine.HistoryLineActivity" />
        <activity android:name=".addBacklogActivity" />
        <activity android:name=".CountDown.CountDownActivity" />
        <activity android:name=".LoginAndSignUp.SignUpActivity" />
        <activity
            android:name=".LoginAndSignUp.LoginActivity"
            android:label="时间备忘录">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="时间备忘录" />
    </application>

</manifest>